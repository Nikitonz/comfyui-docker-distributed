# ------------------------------
# ComfyUI container settings
# ------------------------------
# Use:
# COMPOSE_PROFILES=off  -> run without SMB mount
# COMPOSE_PROFILES=smb  -> run with SMB mount at /mnt/z
COMPOSE_PROFILES=off

COMFY_IMAGE=comfyui:torch2.9
CONTAINER_NAME=comfyui
DOCKER_RESTART=unless-stopped

# Host IP/port for published ComfyUI web port
HOST_BIND_IP=0.0.0.0
COMFY_PORT=8188

EXTRA_MODEL_PATHS=/mnt/z

# ComfyUI runtime params
COMFY_LISTEN_IP=0.0.0.0
COMFY_EXTRA_ARGS="--enable-cors-header"

# ------------------------------
# SMB mount via docker volume driver (cifs)
# Used only when COMPOSE_PROFILES=smb
# ------------------------------
SMB_SERVER=<smb-server-ip-or-hostname>
SMB_SHARE=<smb-share-name>
SMB_USERNAME=<smb-username>
SMB_PASSWORD=<smb-password>
SMB_VERSION=3.0
SMB_MOUNT_FLAGS=ro
SMB_DOMAIN_OPT=
SMB_UID=1000
SMB_GID=1000
SMB_VOLUME_NAME=comfy_models_smb
